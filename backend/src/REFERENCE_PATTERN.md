/**
 * Use this as the reference pattern for every resource.
 * CRITICAL: Files in src/repositories, src/services, src/controllers,
 * src/middleware, src/validators, src/routes are ONE level below src/.
 * To import a module in src/config, src/utils, etc. use "../" NOT "../../".
 * Example: from src/repositories/fooRepo.ts -> import { prisma } from "../config/prisma";
 */
